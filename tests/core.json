{
    "name": {
        "$env": ["APP_NAME", "default"]
    },
    "tag": {
        "key": {
            "$env": "TAG_KEY"
        },
        "value": {
            "$env": ["TAG_VALUE", 3]
        }
    },
    "array": [
        {
            "$env": ["ARRAY_FIRST", 0]
        }
    ],
    "nested": {
        "$env": ["NESTED_FIRST", { "$env": ["NESTED_SECOND", 1] }]
    },
    "content": {
        "$path": "storage/content"
    }
}
